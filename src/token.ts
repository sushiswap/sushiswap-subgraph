// import { ADDRESS_ZERO, MASTER_CHEF_ADDRESS } from './constants'
// import { SushiToken as SushiTokenContract, Transfer as TransferEvent } from '../generated/SushiToken/SushiToken'

// import { getUser } from './masterchef'

// export function transfer(event: TransferEvent): void {
//   if (event.transaction.from != MASTER_CHEF_ADDRESS || event.transaction.to == ADDRESS_ZERO) {
//     return
//   }

//   const user = getUser(event.transaction.to, event.block)
// }
